<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AIGuardian" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Used by SOSWidgetActivity — transparent window so it overlays the home screen -->
    <style name="Theme.AIGuardian.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
