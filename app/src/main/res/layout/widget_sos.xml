<?xml version="1.0" encoding="utf-8"?>
<!--
    Widget layout rendered as RemoteViews (no Compose allowed in widgets).
    Shows a dark card with a red SOS circle button and "Hold 3s" label.
    Clicking anywhere launches SOSWidgetActivity which handles the long-press.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sos_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Card background -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@drawable/widget_bg"
        android:padding="12dp">

        <!-- Red SOS circle -->
        <FrameLayout
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@drawable/sos_circle_bg">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="SOS"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-black" />

        </FrameLayout>

        <!-- Hold label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Hold 3 sec"
            android:textColor="#80FFFFFF"
            android:textSize="10sp" />

    </LinearLayout>

</FrameLayout>
